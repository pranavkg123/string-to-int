name = "pranav"
if len(name) < 3:
    print('The name must be at least three characters.')  # 'print' is now the built-in function
elif len(name) > 50:
    print('The name can be a maximum of 50 characters.')
else:
    print('The name looks good.')